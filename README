Start up commands:
    - ./main [-v] [numPlayers]
    - ./client [serverAddress]

Todo:
    - should probably make a reading thread for each player, if the turn isn't on them, ignore any message they send.